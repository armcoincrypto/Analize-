apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: analize-data-pvc
  namespace: analize
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
